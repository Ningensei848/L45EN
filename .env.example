# 共通
DRY_RUN=false

# Git Config Check
GIT_EXE=%USERPROFILE%\Software\PortableGit\cmd\git.exe
USER_ID={{USER_ID}}

# [DEBUG] for pre-commit hook
DEBUG_TRACE_SETUP=0

# 所有判定の対象: path | remote | both（既定: both）
ID_MATCH_TARGET=both

# コミットメッセージの接頭辞（既定: vault backup）
COMMIT_MESSAGE_PREFIX="vault backup"

# 日付フォーマット（Bashのdate書式。既定: %Y-%m-%d %H:%M:%S）
DATE_FORMAT=%Y-%m-%d %H:%M:%S

# タイムゾーン（例: Asia/Tokyo）。未設定ならPCのタイムゾーンを使用
TIMEZONE=Asia/Tokyo

# .script\__DoNotTouch\SoftwareCheck.ps1 --------------------------------------
# 共有フォルダ（未指定 or 無効なら選択ダイアログが表示）
SHARED_FOLDER="\\server\\share"

# --- Upload Hook Settings (.env) ---
# 共有ルート（UNC禁止のためRドライブ前提）
UPLOAD_ROOT=R:\Upload
# ユーザーID（省略時は %USERNAME% を使用）
# USER_ID=%USERNAME%の末尾
# 画像として扱う拡張子（カンマ区切り）※ svgは含めない
IMAGE_EXTS=.png,.jpg,.jpeg,.gif,.bmp,.tif,.tiff,.webp
# .md の置換対象は「ステージ済みのみ」
MD_STAGED_ONLY=true
# 参照を書換えるか（true固定で運用する想定）
REWRITE_MD=true
# ミラー後に元画像を削除するか（index/作業ツリー両方）
DELETE_ORIGINAL=true
# ログ出力先（リポジトリ直下の既定ファイル名）
LOG_PATH=uploads.log
# コピー判定（hash | mtime）既定 hash：同一内容なら再コピーしない
COPY_MODE=hash
# 既存ファイルがある場合に上書きするか（COPY_MODE=mtime 時の動作に関与）
OVERWRITE=true

# --- Clone-and-Initialize Settings (.env) ---
# UNCパス（Rドライブにマウントする共有）
R_SHARE_UNC="\\server\\share\\repository"
# チーム共有ベアリポジトリ（R:\{TEAM_REPO}.git）
TEAM_REPO=R:\CTH-Nexus.git

# .script\__DoNotTouch\Sync-ObsidianPluginsFromShare.ps1 ------------------------------------------------------
# 必須：共有フォルダのルート
PLUGINS_SOURCE_DIR="\\server\\share\\obsidian\\plugins"
